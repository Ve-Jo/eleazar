import express from 'express';

const clientApiRouter = express.Router();

// Add your API routes here
// Example:
// clientApiRouter.get('/example', (req, res) => {
//   res.json({ message: 'Example route' });
// });

export default clientApiRouter;