[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = [
    "bun install --no-postinstall=false",
    "bunx prisma generate"
]

[start]
cmd = "bun ."

[phases.cache]
directories = ["node_modules", "bun.lockb"]
